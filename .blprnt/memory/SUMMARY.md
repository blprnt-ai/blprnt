# blprnt source summary

- MCP workstream parent is ISSUE-103.
- Current live MCP contract has partially caught up to the intended architecture: resource-specific MCP OpenAPI is exposed at `/api/v1/mcp-servers/openapi.json`, run-scoped enabled-server visibility exists separately, and owner-facing OAuth lifecycle endpoints are now present there.
- Remaining MCP architecture drift: configured MCP servers are still not truly global in the live contract. MCP create/list/read schemas still expose `project_id` semantics (`CreateMcpServerPayload.project_id`, `McpServerDto.project_id`, and `GET /api/v1/mcp-servers?project_id=...`).
- Correct intended contract remains: configured MCP servers are global-only resources; the base prompt should include only a concise global catalog of server names/descriptions; MCP tool definitions should be injected only after explicit run-scoped enablement.
- Follow-up ISSUE-146 was created to remove the project-scoped MCP server contract/persistence drift and align backend/frontend bindings with the clarified global-only requirement.
- 2026-04-08: Closed ISSUE-105 after a verification-only pass confirmed the scoped rmcp/OAuth backend work is now complete. Verified the live resource-specific MCP OpenAPI exposes the owner-facing OAuth lifecycle routes (`/oauth`, `/oauth/launch`, `/oauth/reconnect`, `/oauth/complete`, `/oauth/callback`) and re-ran focused backend checks (`cargo test -p api mcp_oauth_ -- --nocapture`, `cargo test -p adapters mcp::tests:: -- --nocapture`). The remaining MCP backend work is now ISSUE-146 only: remove the still-live project-scoped configured-server contract drift so configured MCP servers are truly global-only.
- 2026-04-08: Continued ISSUE-146 and verified the current source tree now removes project-scoped MCP configured-server contract fields from the active implementation: backend create/list/read/update flows no longer require or emit `project_id`, the repository no longer filters configured servers by project, focused MCP route regression coverage passes, and the frontend MCP settings flow now treats configured servers as global instead of project-selected. Shared project state is not fully closed yet because the running API/OpenAPI at `/api/v1/mcp-servers/openapi.json` is still stale and still advertises `project_id`, and generated backend binding artifacts under `backend/src/bindings/` also still need regeneration to match the cleaned contract.
- 2026-04-08: Another live-contract pass on parent ISSUE-103 confirmed no new MCP planning work is needed; closeout is strictly an ISSUE-146 rollout problem now. The running MCP resource OpenAPI still exposes project-scoped configured-server fields despite the in-tree cleanup, so parent closure must wait for backend regeneration/restart and a final live verification that `project_id` is gone from configured-server contracts.
- 2026-04-08: Continued ISSUE-51 on the Telegram polling-only cleanup path. Shared Telegram backend state now has polling-native regression coverage for link success, reply-context inheritance, and degraded outbound-delivery preservation via `poll_once()`/mocked `getUpdates` instead of the deleted webhook route. Focused polling-native regressions pass individually, but one broader issue fetch/comment/watch regression still needs a clean polling-native rewrite before the grouped polling suite is green end-to-end. Current remaining work here is regression migration cleanup, not a newly reproduced runtime defect.