# Motion Plus registry token (required for pnpm install via .npmrc)
MOTION_PLUS_TOKEN=

# Sentry auth token for source map uploads (required for production builds)
SENTRY_AUTH_TOKEN=

# OpenRouter API key (required at runtime)
OPENROUTER_API_KEY=
