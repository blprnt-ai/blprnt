
Done:
  ✔ The gpt-5 verification error:
  ✘ [ChatStreamResult]: Failed to stream chat: BadRequest: {
    "error": {
      "message": "Your organization must be verified to stream this model. Please go to: https://platform.openai.com/settings/organization/general and click on Verify Organization. If you just verified, it can take up to 15 minutes for access to propagate.",
      "type": "invalid_request_error",
      "param": "stream",
      "code": "unsupported_value"
      }
    }`
  ✔ The file_* tools keep freezing the event loop.
  ✔ Context counting.
  ✔ Add environment details to the prompt.
  ✔ Add a way to delete sessions.
  ✘ Add a confirm modal for closing sessions.
  ✔ Add a strict confirm modal for deleting sessions.
  ✔ Finish the Windows WSL setup.
  ✘ Debug the Windows WSL setup with Max.
    ✘ This needs a proper frontend first.
  ✔ Debug the Linux AppImage setup.
    ~/Desktop$ ./blprnt.AppImage
    /usr/lib/x86_64-linux-gnu/gvfs/libgvfscommon.so: undefined symbol: g_task_set_static_name
    Failed to load module: /usr/lib/x86_64-linux-gnu/gio/modules/libgvfsdbus.so
    /usr/lib/x86_64-linux-gnu/gvfs/libgvfscommon.so: undefined symbol: g_task_set_static_name
    Failed to load module: /usr/lib/x86_64-linux-gnu/gio/modules/libgvfsdbus.so
    /usr/lib/x86_64-linux-gnu/gvfs/libgvfscommon.so: undefined symbol: g_task_set_static_name
    Failed to load module: /usr/lib/x86_64-linux-gnu/gio/modules/libgvfsdbus.so
  ✔ Handle rate-limits, per provider, global singleton for each provider in case multiple sessions are using the same provider.
  ✔ Version the database schema, hard cut over when needed.
  ✔ I re-did the whole fucking provider/auth system. Need to update the frontend to match.
    ✔ Revamp the sidebar menu.
      ✔ Add a "Create Session" button to main screen if there are no sessions.
      ✔ Add a settings page, accessible from the sidebar.
      ✔ Add a session settings page, accessible from the sidebar.
  ✔ Stupid fucking header for project pages should NOT fade/in out when moving between project pages.
  ✔ Add a way to export/import projects.
  ✔ Export project as a JSON file.
  ✔ Import project from a JSON file.
  ✔ Add detailed logging for the providers. Things keep crashing for long running sessions.
  ✔ Refactor sqlite adapter into separate modules.
  ✔ Add CLI args to bindings generation.
  ✔ Debug session status being stuck on first status
  ✔ Fix the auto-scroll on initial render of the output panel.
  ✔ Remove push of history from engine. Convert to pull from FE via tauri command
  ✔ Fix the fucking errors handling in the state frontend

Backlog:
  ✘ Add better retry functionality for the providers.
  ✘ Allow users to add their own models to the catalog
  ✘ Add a way to add custom providers
  ✘ Add translation layer for the provider
  ✘ Add a auth manager for the provider
  ✘ Persist layout to database instead of local storage
  ✘ Implement mpsc for the runtime loop instead of callbacks
  ✘ Aside from planning, knowledge, and memories. Agents will be able to produce artifacts. Not exactly a plan. Not big enough for a knowledge base. And not a memory, since it pertains to the project, not the user.
  ✘ Allow LLM to open a new "tab" to a localhost of the project it's working on, allowing live previews in blprnt
  ✘ Add way to move project management items between projects, roadmaps, specs, tasks
  ✘ Add project/task assignment to session. Assign a roadmap/spec/task to a specific session (or subagent)
  ✘ Agents being able to search their own history, especially after a compact.
  ✘ Agents being able to search through other sessions' history.
  ✔ Use askama crate for templating the prompt.
  ✔ Refactor model definitions to be external as a config in the database
  ✔ Add user-memory to instructions. Have a separate LLM parse out important information and store it in the database then use that in the system prompt.
  [] Figure out a way to allow the llm to use an interactive shell. As in, return an in_progress status to the llm and have the llm respond? Or the user?
  [] Send keystrokes to the shell via keystroke tool???
  [] Guided mode
    [] Guided mode will be a separate LLM that will be used to guide the user through the process of creating a project.
    [] Guided mode will be able to use the same tools as the main LLM, but will be more focused on the user's needs.
    [] Question + answer UI
    [] Next steps UI

Todo:
  [] Add a way to clear the layout in prod. Cause when stuff gets stuck, it's fucked.
  [] If a planning item in the tree is a leaf, no matter the depth, clicking on it should open a modal with the details of the item.

IMPORTANT:
  ✘ Fix the dir_tree tool on windows.
  ✘ Map providers/models to correct reasoning values
  ✘ Add dynamic updater https://v2.tauri.app/plugin/updater/
  ✔ Add the frontend listener for the BetterError emits
  ✔ chat_basic for Gemini
  ✔ Tone down transparency of dialogs
  ✔ Confirm disabled tools DO NOT get sent to the LLM.
  ✔ Add Sentry to frontend
  ✔ Eagerly show user prompt in the output panel.
  ✔ Fix the rate limiter. It's not working as expected.
  ✔ Tokenizer out. [GET]/token_count in.
  ✔ Fix scrolling on the personalities view
  ✔ Add UX indicator when the system is compacting.
  ✔ Fix the timer on the session status.
  ✔ Fix sessions open when project has invalid directories.
  ✔ Fix Gemini
  ✔ Fix OpenAI basic and non-streaming
  ✔ Fix turn/step loop
  ✔ Disambiguate error message and signals.
  ✔ Decouple provider get with api key get.
  ✔ Limit file reads to not explode the context window.
  ☐ Add some messages during pre-turn hooks into the UI. So the user is aware of what's going on.

WISHLIST:
  ✘  Worktrees
  ✘ UI For web searches
  ✔ New UX/UI for subagents
  ✔ Unified view for planning + execution
  ✔ Middle-out context window
  ✔ Define new agents
    x - Deploy
    ✘ - Documentation
    ✘ - Testing
    ✔ - Research
  ✔ Assign a "plan" to a session
  [] Ghost commits
  [] Remove the notion of a "session" ???
  [] Playwright integration
    [] Chrome extension integration - 2-way communication ??
  [] Deploy blprnt built apps to Vercel + Supabase
  [] Mobile app to control desktop app
  ✔ Have a way to rewind subagents
  ✔ New UX/UI for planning
  ✔ Background tasks @started @done
  ✔ Send keystrokes to the TTY shell @started @done
  [] Breadcrumbs for agent sessions. When key decisions are made, the agent will create memories for that decision. We need a way to let other sessions know about these memories. So, having a breadcrumb tip in the system prompt can point the agent to the relevant memories.
  [] I noticed I get better prompt adherence when I say: "Ask me questions. Plan this out" as the last sentence of my first prompt.
    - I'm thinking of injecting this (invisible to the user) as "user message" right after the first user message.
    - But, I'd need to classify the first user message first, to see if it requires a plan.


PROMPS:
  [] Use haiku subagents in parallel to completely map out this codebase then update the agent primer with your compiled results. I want intricate details. Frameworks used. Architecture decision. Coding conventions. Design choices. Colors. Branding. Everything and anything that can be used to bootstrap a new LLM session with enough context to not only create new features but have such an intimate knowledge of the project that it's able to suggest new features. First, do a quick check of the codebase to see how you should sub-divide the work properly.