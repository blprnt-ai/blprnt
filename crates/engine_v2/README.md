# Archived Crate

`engine_v2` is dormant legacy orchestration code kept for reference only.

- It is explicitly excluded from the root Cargo workspace.
- It is not part of the current `blprnt` runtime or release path.
- Live runtime ownership is documented in [`../../docs/runtime-ownership.md`](../../docs/runtime-ownership.md).
