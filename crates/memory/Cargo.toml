[package]
name = "memory"
version = "0.1.0"
edition = "2024"

[dependencies]
events =      { path = "../events" }
persistence = { path = "../persistence" }
shared =      { path = "../shared" }

anyhow =  { workspace = true }
tokio =   { workspace = true }
tracing = { workspace = true }